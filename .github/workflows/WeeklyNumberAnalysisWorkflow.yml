name: Weekly Number Analysis

on:
  schedule:
    - cron: '0 12 * * 1'  # Run every Monday at 12:00 UTC
  workflow_dispatch:  # Allows for manual workflow trigger

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up JDK 23
      uses: actions/setup-java@v2
      with:
        java-version: '23'
        distribution: 'adopt'  # or 'zulu', 'adoptium', 'oracle', etc.
        java-package: jdk

    - name: Build Project
      run: mvn clean package

    - name: Run Analyzer
      run: mvn exec:java -Dexec.mainClass="general_utils.NumberFrequencyAnalyzer"

    - name: Send Email
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.EMAIL_USERNAME }}
        password: ${{ secrets.EMAIL_PASSWORD }}
        subject: 'Weekly Number Analysis'
        from: 'notification.service1988@gmail.com'  # Your sender email address
        html_body: ${{ steps.generate_html_report.outputs.html_report }}  # Use the variable from your report generation step
        to:  ${{ secrets.EMAIL_RECIPIENT }} # Replace with recipient's email
